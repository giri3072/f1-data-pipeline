FROM apache/airflow:2.10.5

# dbt in its own venv so its dependencies never clash with Airflow's
USER root
RUN python -m venv /opt/dbt-venv \
 && /opt/dbt-venv/bin/pip install --no-cache-dir "dbt-postgres>=1.9,<2.0" \
 && chown -R airflow: /opt/dbt-venv
USER airflow